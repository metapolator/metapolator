#!/bin/sh

# Start mysql server
/usr/bin/mysqld_safe &

set -e

cd www/metapolator

app=`find . -name 'metap-run.py' | head -n 1`

if [ -z "$app" ]; then
        echo "Couldn't locate app.py"
        exit 1
fi

python $app